add page for auto add instead of immediate start
    page shows status of run
Add more settings/filter to Auto Add page
    add feature to analyze games before auto-add and get player preferences
    limiting to specific openings
add feature to just think (don't add) on manual stockfish assisted add
    also add better error details on Ivalid PGN on manual stockfish assisted add
    add eval to final terminal output
convert the current pasted moves/pgn/fen to the others on type change
add feature to think (just for the sake of card completion) on full manual add
add ability to duplicate a card in collection view
add ability for user to actually make the move on the board on review
    should more easily store wrong answers for recommended improvements to memorization
add ability to navigate board to set moves to create card
create a "practice mode" for the users to manually navigate lines (using combination of master database and active stockfish)
if best move is a point better than the next after 20 depth, assume it to be better and stop thinking (togglable)
assign cards to opening decks correctly on creation
stockfish assisted manual add using PGN does not work (test FEN too)
On Forced answer page, fix Remove display and drop down display
Remove description column from settings page (because hover hints exist)
add a field to track sibling answers; sibling card should minimize differences
    Field should probably not be a "field"? Should just be a lineage tracker that knows the IDs of other cards
        if it is to stay a field, update the field name, and update background text of Sibling answers field in collection preview (and edit page) to be the ID of sibling cards
    siblings are cards that share a parent. so the previous review position in the move sequence is the same, but the opponent played a different response after it, thus creating a different card
    "sharing a parent" includes transpositions
    it should be preferred that an answer that meets the "other answer" criteria is selected if it is the answer on a sibling card
        in other words, sibling cards should prioritize having the same answer, if possible, so as to reduce the need to memorize more unique cards (if siblings share answers, it is easier to recall that the move you should play next, no matter the opponent response, is the same.)
    not technically "siblings", but if a previous position can be made by playing an other answer, then prefer that answer
    make preferred transpositions and preferred siblings a card creation setting
Implement other flashcard review features from within the Anki app to this app (stats, settings, buttons, customizations, deck options, etc.)
add/remove items from the file,edit,view,window,help bar on tooltips
    ensure that the basic menu buttons are all real features in the app (copy, paste, redo, etc.)
create a "practice mode" for the users to manually navigate lines (using combinationg of master database and active stockfish)
    add ability for user to actually make the move on the board on review (should more easily store wrong answers for recommended improvements to memorization)
cleanup files that aren't needed
Add readme
Create wiki for how to use the app

What should I do when a user created a forced answer of a card after that position already has a different answer
    likely, I should put the previous card and all it's descendants into an Archive folder/tag to remove them from review while they instead review the new forced position and descendants
    however, I imagine that some users will want to review both sets of descendants at a time
    however however, that's a problem because there will exist two cards with identical fronts and different backs, which shouldn't be allowed
Auto Add should only consider creating descendants of unarchived cards?
    if they want to create descendants of archived cards, they can unarchive them, then run auto add.
    I have not yet considered if/how this affects forced answers.

create a "master database" of all cards that users create
    don't keep duplicates; only keep the deepest/most trustworthy result for a position and don't apply sibling assistance to the card stored in the master database
    add feature to offer that users pull their answers from this database instead of using device to think
Add Sync feature for my computer to store their cards
    use the master database to provide the base cade for a given pgn? then their specific cards.json just needs that database id + differences between my card and theirs?

Add daily opening game challenge
    every day, a new sequence of theoretical opponent moves is chosen and one of the best moves is always expected as the answer
    how many moves deep can you make it without messing up
    best/other answers would both be accepted based based on 0.2 acceptance threshold and 20 possible lines max, but the game will always only continue via the user's best line (that way branches don't need to be calculated)
    computer/opponent choice of line will be (seeded and) decided using weighted probability of normal player moves for first 3 moves, then random choice of best/other answers for each move that follows
    pre-calculated moves will only go so deep (~30 moves?), but the user can use their machine to calculate further (then use their machine to share the answer with a server for others to use?)
    log the line that was played with the date it was assigned for users to play previous day's lines
    maybe implement a similar game for endgames and middle games
    add a rating system based on performance in each game over time
    Also track time to solve and add as a 4th dimension of rating (speed rating)
    add as a discord game/activity

convert windows .exe application to run on an iphone/android, mac, linux, and website
    implement cloud sync across devices
    add notifications for reviews or challenges

create my own chess flashcard spacing algorithm (considering the review success of siblings, transpositions, parents, children, and cousins?)
create my own answer-setting algorithm (use answers that the user doesn't even have to "learn")
    tally the count of every position played in every game played (so we now know which positions are most common, when what the user is most likely to play in response to them)
    use that tally to prefer the answers of other cards (ex. after d4, I essentially always play d5. So, as long as d5 is an Other Answer, d5 can safely be set to the actual answer to allow the user to not have to "learn" the answer to that card.)
    Prompt to chatGPT considering the rest of the answer-setting criteria:
        Since a child card is a card that results from playing the next move after the opponents response to the answer of the parent card, a sibling card is a card that shares a parent. So, a sibling card is one that is the result of a different move being played by the opponent after the parent card. 
        Also, when using the Auto Add feature, cards are added to the collection in the order that they are played by the user. So if the user sees the position of a parent card in a real game, and then plays the correct answer to that card in that game, then a child card will also be added depending on what the opponent’s response was to that parent card’s answer. 
        With both of those things being said, when using stockfish to create a new card using this Auto Add feature, I’m envisioning a way of helping the user better memorize answers by setting the answers to moves within the Acceptable Range that are common among already created cards. For example, consider a child card that prefers the answer e5, but would also accept d5. Consider that this card has a sibling that prefers d5, but also accepts e5. I would like to use this information to change the answer to one of the cards so that they agree and the user then doesn’t need to memorize as many unique responses to the parent card; if both cards' answers were set to e5, the user could simply memorize that, no matter what the opponent plays in response to the parent card, they can always play e5. 
        The catch is that I also don’t want to change the answer to a card by surprise on the user. I don’t want them to learn d5 is the answer, then have the Auto Add feature change the already learned card to e5 and confuse the user. So, this consolidating of child answers would likely need to happen upon the creation of the second card (and then other future cards too). 
        I also want to consider if using any of the Other Answer fields as the “best” answer on creation would put the board into a position that already exists in the collection (essentially, have the card creation script prefer to create transpositions in another effort to ease the memorization capacity of the user). 
        On top of those more subjective ways of deciding what the “best” answer is for a card, I also already force the answer to be a certain answer if the user says to, or if that position has already been reached in a card using a different move sequence (transposition) to ensure that transpositions have identical answers (this is different from “preferring the creation of transpositions”; rather, this is essentially “if a transposition is made, seeing the opponent play e5 in response to either parent transposition MUST create cards with identical answers” or “since the current position was the same, the response to either previous move must be the same too”). 
        What is the best way to implement this? With so many things being considered on card creation, how do we pick the simplest “best” answer each time from the subset of Other Answers while also ensuring that reasons for forcing an answer should trump subjective reasons to chose a best answer?